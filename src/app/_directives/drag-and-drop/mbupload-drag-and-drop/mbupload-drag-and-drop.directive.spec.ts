import { MBUploadDragAndDropDirective } from './mbupload-drag-and-drop.directive';

describe('MBUploadDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new MBUploadDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
