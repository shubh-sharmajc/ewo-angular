import {CheckLoginClickDirective} from './check-login-click.directive';

describe('CheckLoginClickDirective', () => {
  it('should create an instance', () => {
    // const directive = new CheckLoginClickDirective();
    // expect(directive).toBeTruthy();
  });
});
